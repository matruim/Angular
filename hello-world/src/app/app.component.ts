import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'] // could use styles:[] instead of the url if you have less styles
})
export class AppComponent {
}
